# Honeywell BA RAG System — Python dependencies
# Install with:
#   pip install -r requirements.txt --break-system-packages

# PDF text extraction
pymupdf>=1.23.0

# Excel reading
openpyxl>=3.1.0

# Metadata index (TF-IDF)
scikit-learn>=1.3.0
numpy>=1.24.0

# Data handling
pandas>=2.0.0

# LLM — Groq (cloud, fast, free tier)
# Get key: console.groq.com
groq>=0.4.0

# LLM — Ollama runs separately (no pip package needed)
# Install: https://ollama.ai
# Models:  ollama pull qwen2.5:7b
#          ollama pull mistral
#          ollama pull tinyllama
